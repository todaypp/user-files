# Common
sudo apt update
sudo apt install -y build-essential htop nvim redis

# NVM

# Java
sudo apt install openjdk-8-jdk

# Python
sudo apt install python python3

# Copying config files
cp .gitconfig .tmux.conf .vimrc ~
